import { createSlice } from "@reduxjs/toolkit";
const initialState: {}[] = [];
const basketAsteroidsSlice = createSlice({
  name: "basketAsteroids",
  initialState,
  reducers: {},
});
export default basketAsteroidsSlice;
